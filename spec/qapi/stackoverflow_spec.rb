require 'spec_helper'

describe Qapi::Stackoverflow do
  it 'has a version number' do
    expect(Qapi::Stackoverflow::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
